# My First Steps in Python 🚀

Este repositório contém meus primeiros passos na programação, focando em **Clean Code** e **Lógica de Decisão**.

## 🧠 O que aprendi até agora:
- **Operadores de Identidade:** Uso de `in` e `not in` para buscas em listas.
- **Escrita Enxuta (Ternários):** Como transformar blocos de `if/else` em linhas únicas e legíveis.
- **Manipulação de Listas:** Métodos `.append()`, `.remove()` e a função `len()`.
- **Boas Práticas:** Nomeação de variáveis descritivas e organização lógica.

## 📂 Estrutura de Arquivos:
- `controle_estoque.py`: Gerenciamento de listas de compras e produtos.
- `validacao_acesso.py`: Lógica de permissões e cupons de desconto.
